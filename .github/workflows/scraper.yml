name: CityBike Lima Scraper

on:
  schedule:
    - cron: "*/30 * * * *"   # cada 30 minutos
  workflow_dispatch:          # ejecución manual opcional

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests beautifulsoup4 lxml openpyxl selenium webdriver-manager

      - name: Ejecutar snapshot
        env:
          OWM_KEY: ${{ secrets.OWM_KEY }}
        run: python collector.py

      - name: Commit y push resultados
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add data/citybike_lima.csv data/citybike_lima.xlsx || true
          git commit -m "Snapshot actualizado $(date)" || echo "No hay cambios que commitear"
          git push origin HEAD:main
        continue-on-error: true
